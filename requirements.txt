# --- Core GUI / DB ---
PyQt6==6.5.2
PyQt6-Qt6==6.5.2
PyQt6-sip>=13.5,<14
SQLAlchemy==2.0.23

# --- PDF receipts & printing ---
reportlab==4.0.7
pillow>=10.0.0

# --- ESC/POS thermal printer ---
python-escpos>=3.0.0
pyusb>=1.2.1          # needed if you use Usb() backend (XP printers)

# --- Arabic text shaping ---
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# --- Optional utilities (recommended) ---
pyserial>=3.5          # for serial/COM printers (some XP models)
cupshelpers>=1.0       # for 'lp' fallback on Linux
